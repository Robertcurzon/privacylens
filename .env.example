# Optional. Used for stable HMAC fingerprints and hashed replacements.
# Use a long random value in production. Raw values are never written to reports.
PRIVACYLENS_HASH_KEY=

# Runtime artifact directory for hosted deployments.
PRIVACYLENS_DATA_DIR=data/runtime
